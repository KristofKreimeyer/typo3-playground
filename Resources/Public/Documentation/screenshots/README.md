# Screenshot assets

Place the portfolio screenshots in this directory using these filenames:

- `backend-module-overview.png`
- `hero-desktop-preview.png`
- `mobile-preview.png`
- `validation-error.png`

The repository README already references these paths. Capture the images from the local TYPO3 backend; do not substitute generated or mock screenshots.
